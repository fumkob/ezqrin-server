# ezQRin Server - Secret Environment Variables
#
# IMPORTANT: Copy this file to .env.secrets and update with your actual values
# NEVER commit .env.secrets file to version control
#
# This file contains only SECRET credentials that should never be committed.
# Non-secret configuration is in config/default.yaml, config/development.yaml, etc.

#
# Database Credentials
#
DB_USER=your-database-user
DB_PASSWORD=your-secure-database-password
DB_NAME=your-database-name

#
# JWT Secret
#
# CRITICAL: Use a cryptographically secure random string (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE-ME-use-openssl-rand-base64-32-to-generate

#
# QR Code HMAC Secret
#
# CRITICAL: Used to sign and verify QR codes. Must be at least 32 characters.
# Must match QR_HMAC_SECRET on the QR hosting server if used.
# Generate with: openssl rand -base64 32
QR_HMAC_SECRET=CHANGE-ME-use-openssl-rand-base64-32-to-generate

#
# Redis Password (Optional)
#
REDIS_PASSWORD=

# ============================================================================
# QUICK START FOR DEVCONTAINER
# ============================================================================
# If you're using DevContainer (.devcontainer/), you DON'T need this file.
# DevContainer sets these values automatically via docker-compose.yaml:
#
#   DB_USER=ezqrin
#   DB_PASSWORD=ezqrin_dev
#   DB_NAME=ezqrin_db
#   JWT_SECRET=dev-secret-key-minimum-32-chars-long-for-development-only
#   QR_HMAC_SECRET=dev-qr-hmac-secret-key-minimum-32-chars-long-for-development-only
#
# This file is only needed for local development outside DevContainer.
# ============================================================================

# ============================================================================
# DEVELOPMENT VALUES (for local development)
# ============================================================================
# For local development outside DevContainer, use these values:
#
#   DB_USER=ezqrin
#   DB_PASSWORD=ezqrin_dev
#   DB_NAME=ezqrin_db
#   JWT_SECRET=dev-secret-key-minimum-32-chars-long-for-development-only
#   QR_HMAC_SECRET=dev-qr-hmac-secret-key-minimum-32-chars-long-for-development-only
#   REDIS_PASSWORD=
# ============================================================================
